# Harvest AI — Architecture Diagram

> Presentation & Demonstration Reference
> Region: `ap-southeast-2` (Sydney) · Stack: AWS CDK · Model: Amazon Bedrock (Claude 3.5 Sonnet)

---

## 1. System Overview

```
╔══════════════════════════════════════════════════════════════════════════════════════╗
║                              HARVEST AI PLATFORM                                    ║
║              Agentic AI Platform for Agriculture · Built on AWS                     ║
╚══════════════════════════════════════════════════════════════════════════════════════╝

 ┌─────────────────────────────────────────────────────────────────────────────────┐
 │                            USERS (Farmers / Agronomists)                        │
 │                    Browser · Mobile · Voice Interface                           │
 └───────────────────────────────────┬─────────────────────────────────────────────┘
                                     │ HTTPS
                                     ▼
 ┌─────────────────────────────────────────────────────────────────────────────────┐
 │                         FRONTEND LAYER                                          │
 │                                                                                 │
 │   React 19 + Vite + TypeScript                                                  │
 │   ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────┐ ┌─────────┐ │
 │   │   Weather    │ │  AI Crop     │ │    Farm      │ │  Market  │ │  Voice  │ │
 │   │  Dashboard   │ │  Diagnosis   │ │   Planner    │ │  Prices  │ │Assistant│ │
 │   └──────────────┘ └──────────────┘ └──────────────┘ └──────────┘ └─────────┘ │
 │                                                                                 │
 │   ┌──────────────────────────────────────────────────────────────────────────┐  │
 │   │  Auth UI — /login · /signup · Auth-aware Navbar                         │  │
 │   │  (UI-only mock, localStorage persistence · Cognito integration pending)  │  │
 │   └──────────────────────────────────────────────────────────────────────────┘  │
 │                                                                                 │
 │   Hosted on:  AWS Amplify ──► CloudFront CDN ──► S3 (static assets)            │
 └───────────────────────────────────┬─────────────────────────────────────────────┘
                                     │ REST API
                                     ▼
 ┌─────────────────────────────────────────────────────────────────────────────────┐
 │                         API GATEWAY LAYER                                       │
 │                        Amazon API Gateway (REST)                                │
 │                       CORS enabled · Stage per env                              │
 │                                                                                 │
 │  /api/weather/*  /api/diagnosis/*  /api/farm/*  /api/market/*  /api/voice/*    │
 └──┬───────────────┬──────────────────┬────────────┬─────────────┬───────────────┘
    │               │                  │            │             │
    ▼               ▼                  ▼            ▼             ▼
 ┌──────────┐  ┌──────────┐  ┌──────────────┐  ┌──────────┐  ┌──────────┐
 │ Weather  │  │Diagnosis │  │    Farm      │  │  Market  │  │  Voice   │
 │ Lambdas  │  │ Lambdas  │  │ Rec. Lambda  │  │  Lambda  │  │  Lambda  │
 └──────────┘  └──────────┘  └──────────────┘  └──────────┘  └──────────┘
    │               │                  │            │             │
    └───────────────┴──────────────────┴────────────┴─────────────┘
                                       │
                                       ▼
 ┌─────────────────────────────────────────────────────────────────────────────────┐
 │                            AI / DATA LAYER                                      │
 │                                                                                 │
 │  ┌──────────────────────────────────────────┐   ┌────────────────────────────┐ │
 │  │           Amazon Bedrock                  │   │    External Data Sources   │ │
 │  │   Claude 3.5 Sonnet (APAC)               │   │                            │ │
 │  │   Model ID: claude-3-5-sonnet-20241022   │   │  OpenWeatherMap API        │ │
 │  │                                           │   │  World Bank Commodity API  │ │
 │  │   · Farming advisory generation          │   │                            │ │
 │  │   · Crop disease diagnosis               │   └────────────────────────────┘ │
 │  │   · Farm planting recommendations        │                                  │
 │  │   · Market insight summaries             │   ┌────────────────────────────┐ │
 │  │   · Voice assistant responses            │   │       AWS Storage          │ │
 │  └──────────────────────────────────────────┘   │                            │ │
 │                                                  │  DynamoDB  ·  S3          │ │
 │                                                  └────────────────────────────┘ │
 └─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Detailed AWS Infrastructure per Feature

### 2.1 Weather Intelligence Agent

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                     WEATHER INTELLIGENCE AGENT                               │
  └──────────────────────────────────────────────────────────────────────────────┘

  External Source           EventBridge              Lambda Functions
  ┌────────────────┐        ┌─────────────┐          ┌──────────────────────────┐
  │ OpenWeatherMap │        │  Rule:      │          │  WeatherPolling Lambda   │
  │ API            │◄───────│  every 30   │─────────►│  (scheduled trigger)     │
  │ (Forecast +    │        │  minutes    │          │  · fetch forecast        │
  │  Alerts)       │        └─────────────┘          │  · evaluate thresholds   │
  └────────────────┘                                 │  · persist alerts        │
                                                     │  · invoke Bedrock        │
                                                     └─────────┬────────────────┘
                                                               │
                         ┌─────────────────────────────────────┤
                         │                                     │
                         ▼                                     ▼
              ┌──────────────────┐                  ┌──────────────────────┐
              │    DynamoDB      │                  │   Amazon Bedrock     │
              │  HarvestAI-      │                  │   Claude 3.5 Sonnet  │
              │  {stage}-        │                  │   · Daily farming    │
              │  WeatherAlerts   │                  │     advisory text    │
              │  PK: userId      │                  └──────────────────────┘
              │  SK: alertId     │
              └──────────────────┘
                         │
                         ▼
              ┌──────────────────┐
              │   Amazon SNS     │          Farmers
              │  HarvestAI-      │─────────►  📧 Email
              │  {stage}-        │─────────►  📱 SMS
              │  WeatherAlerts   │
              └──────────────────┘

  API Gateway Routes (user-facing):
  ┌────────────────────────────────────────────────────────────────┐
  │  GET  /api/weather/forecast      → WeatherForecast Lambda      │
  │  GET  /api/weather/alerts        → WeatherAlerts Lambda        │
  │  PUT  /api/weather/alerts/{id}/acknowledge → Acknowledge Lambda│
  │  POST /api/weather/subscribe     → WeatherSubscription Lambda  │
  └────────────────────────────────────────────────────────────────┘

  Alert Thresholds:  > 35°C (high temp)  ·  < 5°C (frost)
                     > 50mm rain (flood)  ·  7+ dry days (drought)
```

---

### 2.2 AI Crop Diagnosis Chatbot

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                       AI CROP DIAGNOSIS CHATBOT                              │
  └──────────────────────────────────────────────────────────────────────────────┘

  User (farmer)
      │ 1. Upload photo
      │ 2. Describe symptoms
      ▼
  API Gateway
      │
      ├──────────────────────────────────────────────────────────────────────┐
      │                                                                      │
      ▼                                                                      ▼
  ┌──────────────────────────┐                              ┌────────────────────┐
  │   Diagnosis Lambda       │                              │  Upload Lambda     │
  │   · Chat orchestration   │                              │  · Generate        │
  │   · Session management   │                              │    presigned S3    │
  │   · Invoke Bedrock       │                              │    URL             │
  └──────────┬───────────────┘                              └────────┬───────────┘
             │                                                       │
             ├──────────────────────┐                               │
             │                      │                               ▼
             ▼                      ▼                    ┌──────────────────────┐
  ┌──────────────────┐   ┌──────────────────┐           │        S3            │
  │    DynamoDB      │   │  Amazon Bedrock  │           │  Crop photos / media │
  │  ChatSessions    │   │  Claude 3.5      │           │  (presigned upload)  │
  │  · Session state │   │  · Photo analysis│           └──────────────────────┘
  │  · Chat history  │   │  · Diagnosis     │
  │  · Last diagnosis│   │  · Treatment     │
  │  TTL auto-expiry │   │    recommendations│
  └──────────────────┘   └──────────────────┘

  API Gateway Routes:
  ┌────────────────────────────────────────────────────────────────┐
  │  POST /api/diagnosis/chat       → Diagnosis Lambda             │
  │  GET  /api/diagnosis/sessions   → Sessions Lambda              │
  │  POST /api/diagnosis/upload     → Upload Lambda (presign URL)  │
  └────────────────────────────────────────────────────────────────┘

  Diagnosis Types:  Disease · Pest damage · Nutrient deficiency · Abiotic stress
```

---

### 2.3 Farm Planning Advisor

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                        FARM PLANNING ADVISOR                                 │
  └──────────────────────────────────────────────────────────────────────────────┘

  User Input                Lambda                   Bedrock
  ┌──────────────┐         ┌───────────────────┐    ┌────────────────────────┐
  │ Farm details │         │  FarmRecommendation│    │   Claude 3.5 Sonnet    │
  │ · Farm type  │────────►│  Lambda            │───►│   · Analyse farm data  │
  │ · Soil type  │         │  · Build prompt    │    │   · Generate crop list │
  │ · Location   │         │  · Parse response  │    │   · Score suitability  │
  │ · Climate    │         │  · Format results  │◄───│   · Estimate yields    │
  │ · Budget     │         └───────────────────┘    └────────────────────────┘
  │ · Season     │
  └──────────────┘
        ▲
        │ Results
  ┌──────────────┐
  │ Ranked list  │
  │ Crop / live- │
  │ stock recs   │
  │ with scores  │
  └──────────────┘

  API Gateway Route:
  ┌────────────────────────────────────────────────────────────────┐
  │  POST /api/recommendations  → FarmRecommendation Lambda        │
  └────────────────────────────────────────────────────────────────┘
```

---

### 2.4 Market Price Intelligence

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                      MARKET PRICE INTELLIGENCE                               │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌────────────────────┐       ┌───────────────────┐      ┌────────────────────┐
  │  World Bank        │       │  MarketPrice       │      │  Amazon Bedrock    │
  │  Commodity API     │──────►│  Lambda            │─────►│  Claude 3.5 Sonnet │
  │  · Crop prices     │       │  · Fetch prices    │      │  · Market insight  │
  │  · Price trends    │       │  · Calculate trends│      │    summaries       │
  │  · Historical data │       │  · Invoke Bedrock  │◄─────│  · Price analysis  │
  └────────────────────┘       └───────────────────┘      └────────────────────┘

  API Gateway Routes:
  ┌────────────────────────────────────────────────────────────────┐
  │  GET  /api/market/prices        → MarketPrice Lambda           │
  │  POST /api/market/insight       → MarketPrice Lambda           │
  └────────────────────────────────────────────────────────────────┘
```

---

### 2.5 Multilingual Voice Assistant

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                     MULTILINGUAL VOICE ASSISTANT                             │
  └──────────────────────────────────────────────────────────────────────────────┘

  Farmer speaks          Speech-to-Text           AI Processing
  ┌────────────┐        ┌──────────────────┐     ┌──────────────────────────┐
  │ 🎙️ Voice  │───────►│ AWS Transcribe   │────►│   Amazon Bedrock         │
  │   input    │        │ Streaming        │     │   Claude 3.5 Sonnet      │
  └────────────┘        │ Real-time STT    │     │   · Understand query     │
                        └──────────────────┘     │   · Generate response    │
                                                 │   · In user's language   │
                        ┌──────────────────┐     └────────────┬─────────────┘
  ┌────────────┐        │ AWS Polly        │◄───────────────  │
  │ 🔊 Audio  │◄───────│ Text-to-Speech   │                   │
  │  response  │        │ Natural voice    │    ┌─────────────▼────────────┐
  └────────────┘        │ output           │    │  Voice Lambda            │
                        └──────────────────┘    │  · Orchestrate STT/TTS   │
                                                │  · Session management    │
                                                └──────────────────────────┘

  Supported Languages:
  🇦🇺 English  ·  🇨🇳 Chinese (Simplified/Traditional)  ·  🇮🇳 Hindi
  🇸🇦 Arabic   ·  🇻🇳 Vietnamese  ·  🇮🇹 Italian

  API Gateway Route:
  ┌────────────────────────────────────────────────────────────────┐
  │  POST /api/voice/chat  → Voice Lambda                          │
  └────────────────────────────────────────────────────────────────┘
```

---

## 3. AWS Infrastructure Stack Map

```
╔══════════════════════════════════════════════════════════════════════════════════╗
║              AWS ACCOUNT  ·  Region: ap-southeast-2 (Sydney)                   ║
╠══════════════════════════════════════════════════════════════════════════════════╣
║                                                                                 ║
║  ┌──────────────────────────────┐   ┌──────────────────────────────────────┐  ║
║  │  WEATHER STACK               │   │  DIAGNOSIS STACK                     │  ║
║  │  HarvestAI-{stage}-Weather   │   │  HarvestAI-{stage}-Diagnosis         │  ║
║  │                              │   │                                      │  ║
║  │  Lambda (×5)                 │   │  Lambda (×3)                         │  ║
║  │  · WeatherForecast           │   │  · DiagnosisChat                     │  ║
║  │  · WeatherAlerts             │   │  · DiagnosisSessions                 │  ║
║  │  · WeatherAcknowledge        │   │  · DiagnosisUpload                   │  ║
║  │  · WeatherSubscription       │   │                                      │  ║
║  │  · WeatherPolling            │   │  DynamoDB                            │  ║
║  │                              │   │  · ChatSessions table                │  ║
║  │  DynamoDB                    │   │                                      │  ║
║  │  · WeatherAlerts table       │   │  S3                                  │  ║
║  │                              │   │  · harvest-ai-media bucket           │  ║
║  │  SNS Topic                   │   │                                      │  ║
║  │  · WeatherAlerts             │   │  API Gateway                         │  ║
║  │                              │   │  · /api/diagnosis/*                  │  ║
║  │  EventBridge Rule            │   └──────────────────────────────────────┘  ║
║  │  · Every 30 mins poll        │                                             ║
║  │                              │   ┌──────────────────────────────────────┐  ║
║  │  API Gateway                 │   │  SHARED SERVICES (All Features)      │  ║
║  │  · /api/weather/*            │   │                                      │  ║
║  └──────────────────────────────┘   │  Amazon Bedrock                      │  ║
║                                     │  · Claude 3.5 Sonnet (APAC)         │  ║
║  ┌──────────────────────────────┐   │  · claude-3-5-sonnet-20241022-v2    │  ║
║  │  FRONTEND HOSTING            │   │                                      │  ║
║  │                              │   │  Amazon Cognito                      │  ║
║  │  AWS Amplify                 │   │  · harvest-ai-users pool             │  ║
║  │  · Auto build on push        │   │  · JWT authentication                │  ║
║  │  · Branch previews           │   │                                      │  ║
║  │                              │   │  AWS Polly        (Voice TTS)        │  ║
║  │  CloudFront CDN              │   │  AWS Transcribe   (Voice STT)        │  ║
║  │  · Global edge caching       │   │                                      │  ║
║  │  · HTTPS / SSL               │   │  IAM Roles (OIDC for GitHub Actions) │  ║
║  │                              │   └──────────────────────────────────────┘  ║
║  │  S3 (static assets)          │                                             ║
║  └──────────────────────────────┘                                             ║
╚══════════════════════════════════════════════════════════════════════════════════╝

  Runtime:     Node.js 22.x Lambda (esbuild bundled, no Docker)
  IaC:         AWS CDK (TypeScript)
  Billing:     DynamoDB Pay-per-request  ·  Lambda per-invocation
  Recovery:    DynamoDB PITR enabled on all tables
               Prod tables: RemovalPolicy.RETAIN
```

---

## 4. CI/CD Pipeline

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                        GITHUB ACTIONS CI/CD PIPELINE                         │
  └──────────────────────────────────────────────────────────────────────────────┘

  ─────────────────── EVERY PUSH (ci.yml) ───────────────────────────────────────

  Push to any branch
        │
        ▼
  ┌─────────────────────────────────────────────────────────────────────┐
  │                        CI GATE (Parallel)                           │
  │                                                                     │
  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
  │  │   Lint & Format  │  │   Type Check     │  │ Security Audit   │ │
  │  │   ESLint v9      │  │   tsc --noEmit   │  │ npm audit        │ │
  │  │   Prettier       │  │   (all 4 pkgs)   │  │ --audit-level=   │ │
  │  └────────┬─────────┘  └────────┬─────────┘  │ high            │ │
  │           │                     │             └──────────────────┘ │
  │           └─────────┬───────────┘                                  │
  │                     ▼                                              │
  │  ┌──────────────────────────────────────┐                         │
  │  │         Tests (Parallel)              │                         │
  │  │  ┌───────────────┐ ┌───────────────┐ │                         │
  │  │  │ Backend Tests │ │Frontend Tests │ │                         │
  │  │  │ Jest          │ │ Vitest        │ │                         │
  │  │  └───────┬───────┘ └───────┬───────┘ │                         │
  │  └──────────┼─────────────────┼─────────┘                         │
  │             └────────┬────────┘                                    │
  │                      ▼                                             │
  │  ┌───────────────────────────────────┐                            │
  │  │  Build All Workspaces             │                            │
  │  │  shared → backend → frontend      │                            │
  │  │  Upload build artifacts           │                            │
  │  └───────────────────────────────────┘                            │
  └─────────────────────────────────────────────────────────────────────┘

  ─────────────── PUSH TO dev/staging (deploy-backend.yml) ──────────────────────

  Push to dev or staging branch
        │
        ├── CI Gate (above, runs first)
        │
        ▼
  ┌───────────────────┐
  │  Resolve Stage    │   dev → stage=dev   ·   staging → stage=staging
  └────────┬──────────┘
           │
           ▼
  ┌───────────────────────────────────────┐
  │  CDK Deploy (AWS OIDC auth)           │
  │  · cdk diff (preview changes)         │
  │  · cdk deploy --all                   │
  │  · Outputs: API URL, table names, etc.│
  └────────┬──────────────────────────────┘
           │
           ▼
  ┌───────────────────────────────────────┐
  │  Frontend Deploy (S3 + CloudFront)    │
  │  · Build React app (VITE_* env vars)  │
  │  · aws s3 sync (hashed assets → 1yr) │
  │  · index.html (no-cache)             │
  │  · CloudFront invalidation           │
  └────────┬──────────────────────────────┘
           │
           ▼
  ┌───────────────────────────────────────┐
  │  Health Check                         │
  │  · API /health endpoint               │
  │  · Frontend HTTP 200 check            │
  └───────────────────────────────────────┘

  ─────────────── PUSH TO main (deploy-production.yml) ──────────────────────────

  Push to main (merge PR)
        │
        ├── CI Gate (above)
        │
        ▼
  ┌───────────────────────────────────────┐
  │  Production Deploy                    │
  │  ⚠️  Requires manual approval in       │
  │  GitHub Environments → "production"  │
  └────────┬──────────────────────────────┘
           │
           ├── CDK Deploy (stage=prod)
           │      └── Tables: RemovalPolicy.RETAIN
           │
           ├── Frontend Deploy (S3 + CloudFront)
           │
           ▼
  ┌───────────────────────────────────────┐
  │  Smoke Tests (per feature)            │
  │  · /weather/current      ✓            │
  │  · /diagnosis/health     ✓            │
  │  · /recommendations/health ✓          │
  │  · /market/health        ✓            │
  │  · /voice/health         ✓            │
  └────────┬──────────────────────────────┘
           │
           ▼
  ┌───────────────────────────────────────┐
  │  GitHub Release                       │
  │  · Auto-tags: v2026.02.21.{run}       │
  │  · Creates release notes with all     │
  │    features deployed                  │
  └───────────────────────────────────────┘

  Concurrency guards:  No two production deploys run simultaneously.
```

---

## 5. Authentication & Security

```
  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                       AUTHENTICATION & SECURITY                              │
  └──────────────────────────────────────────────────────────────────────────────┘

  User                 Frontend              Cognito              API Gateway
   │                      │                     │                      │
   │── Sign In ──────────►│                     │                      │
   │                      │── Authenticate ────►│                      │
   │                      │◄── JWT tokens ──────│                      │
   │                      │                     │                      │
   │── API request ───────►── Bearer token ──────────────────────────►│
   │                      │                     │     Validate JWT ───►│
   │                      │                     │◄── Authorized ───────│
   │                      │                     │                      │── Lambda
   │◄─── Response ─────────────────────────────────────────────────────│

  ┌─────────────────────────────────────────────────────────────────────┐
  │  AWS OIDC (GitHub Actions → AWS)                                    │
  │                                                                     │
  │  GitHub Actions  ──OIDC token──►  IAM Role (AWS_DEPLOY_ROLE_ARN)   │
  │                                    · No long-lived credentials      │
  │                                    · Least-privilege CDK perms      │
  └─────────────────────────────────────────────────────────────────────┘

  Secrets managed in:  GitHub Secrets (CI/CD) · AWS Secrets Manager (runtime)
  API Keys:            WEATHER_API_KEY stored as Lambda env var via CDK
```

---

## 6. Monorepo Structure

```
  harvest-ai/                          npm workspace monorepo
  │
  ├── frontend/                        @harvest-ai/frontend
  │   ├── src/
  │   │   ├── pages/                   WeatherPage, DiagnosisPage, SignInPage, SignUpPage, ...
  │   │   ├── components/              WeatherCard, ChatBubble, ...
  │   │   ├── hooks/                   useWeatherData, useDiagnosis, useAuth, ...
  │   │   ├── services/                weather.service.ts, diagnosisApi.ts
  │   │   └── styles/                  CSS variables / design tokens
  │   └── vite.config.ts
  │
  ├── backend/                         @harvest-ai/backend
  │   └── src/
  │       ├── handlers/                Lambda entry points per feature
  │       │   ├── weather/             weatherForecast, weatherAlert, ...
  │       │   ├── diagnosis/           diagnosis, sessions, upload
  │       │   ├── farm-recommendation/ farmRecommendation
  │       │   ├── market-price/        marketPrice
  │       │   └── voice/               voiceChat
  │       ├── agents/                  WeatherAgent (Bedrock orchestration)
  │       ├── services/                Business logic per domain
  │       ├── repositories/            DynamoDB data access
  │       └── models/                  Data transformation models
  │
  ├── shared/                          @harvest-ai/shared
  │   └── src/
  │       ├── weather.types.ts         Shared API contracts
  │       ├── diagnosis.types.ts
  │       ├── farmRecommendation.types.ts
  │       ├── marketPrice.types.ts
  │       ├── voice.types.ts
  │       └── voice.constants.ts
  │
  ├── infrastructure/                  @harvest-ai/infrastructure
  │   └── lib/
  │       ├── stacks/                  WeatherStack, DiagnosisStack
  │       └── constructs/              HarvestLambda, HarvestTable
  │
  └── .github/workflows/
      ├── ci.yml                       Lint → Test → Build
      ├── deploy-backend.yml           Dev/Staging CDK + S3 deploy
      └── deploy-production.yml        Prod deploy + smoke tests + release
```

---

## 7. Technology Stack Summary

```
  ┌────────────────────────────────────────────────────────────────────────────┐
  │                        TECHNOLOGY STACK                                    │
  ├───────────────────┬────────────────────────────────────────────────────────┤
  │  Layer            │  Technology                                            │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  Frontend         │  React 19 · Vite · TypeScript · CSS Modules            │
  │  Routing          │  React Router v6                                       │
  │  i18n             │  i18next (7 languages)                                 │
  │  Charts           │  Recharts                                              │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  Backend          │  TypeScript · Node.js 22 · AWS Lambda (Serverless)     │
  │  HTTP (local)     │  Express (mock server only)                            │
  │  Bundler          │  esbuild (via CDK NodejsFunction)                      │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  AI               │  Amazon Bedrock — Claude 3.5 Sonnet (APAC)            │
  │  Voice STT        │  AWS Transcribe Streaming                              │
  │  Voice TTS        │  AWS Polly                                             │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  Database         │  Amazon DynamoDB (Pay-per-request · PITR enabled)      │
  │  Storage          │  Amazon S3 (crop photos · static assets)               │
  │  Notifications    │  Amazon SNS (email + SMS alerts)                       │
  │  Scheduling       │  Amazon EventBridge (30-min weather polling)           │
  │  Auth             │  Amazon Cognito (JWT)                                  │
  │  API              │  Amazon API Gateway (REST · CORS)                      │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  Hosting          │  AWS Amplify → CloudFront CDN → S3                    │
  │  IaC              │  AWS CDK (TypeScript)                                  │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  CI/CD            │  GitHub Actions (3 workflows)                          │
  │  Testing          │  Jest (backend) · Vitest (frontend)                    │
  │  Linting          │  ESLint v9 · Prettier                                  │
  │  Type checking    │  TypeScript strict mode (all 4 workspaces)             │
  ├───────────────────┼────────────────────────────────────────────────────────┤
  │  Environments     │  dev → staging → production (3 stages)                 │
  │  Region           │  ap-southeast-2 (Sydney, Australia)                    │
  └───────────────────┴────────────────────────────────────────────────────────┘
```
