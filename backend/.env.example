# Set to "true" to use real AWS Bedrock + Transcribe instead of mocks
USE_REAL_BEDROCK=true

# AWS region where your Bedrock models are enabled
AWS_REGION=ap-southeast-2

# Port for the local dev server
PORT=3000
